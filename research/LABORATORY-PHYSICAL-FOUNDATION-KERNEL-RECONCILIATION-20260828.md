# Laboratory Physical Foundation Kernel Reconciliation — 2026-08-28

## Standing

**EXACT HISTORICAL FOUNDATIONS CANONICALIZED / CURRENT ROLES RECONCILED**

A detached-lineage audit found 30 older `LABORATORY-*` files absent from current main. Wholesale restoration would recreate stale locality, product and family-status claims. The current physical bridge needs a much smaller derivation kernel. Eight files are therefore restored byte-for-byte from `22f6c33`; their dated `CURRENT` labels remain historical text rather than 2026-08-28 current standing.

## Restored kernel

```text
Physical Falsifier Map
E0/E1 Physical Falsification Programme v1.0 FROZEN
F02 Physical Safety / Authority / Energy Isolation
F05 Device Interoperability / Identity / Lifecycle
F06 Electronics / Embedded Instrumentation
F11 Physical Object / Sample / Material Lifecycle
F12 Metrology / Calibration / Reference Chains
F14 Evidence / Provenance / Data Continuity
```

Exact source revision, Git blob, byte count and SHA-256 are retained in the machine-readable reconciliation receipt.

## Current authority order

The restored files are **derivation foundations**, not a second current-state system:

```text
physical Reality / owner evidence
> current E0-B prereg + re-entry + information-contrast contracts
> current Workstation attachment/provider projection
> current Runtime / Host / owner receipts
> dated physical-foundation derivation documents
```

### Physical Falsifier Map

Its PF distinctions and physical-pressure reasoning remain foundational. It is also a direct source referenced by the current E0-B evidence receipt. Its older sequential-family next step does not override the current synthetic-floor closeout or R0 staging.

### Frozen E0/E1 programme

This restores the definitions behind the `E0-A → E0-E` names still used by the canonical first-order packet. The programme remains a useful falsification plan, but E0-B v0.3 now owns the exact current first-bridge carrier, blinding, measurement-information and re-entry semantics. E1 remains JIT.

### F02 / F05 / F06

Retain the deeper distinctions needed by real physical execution:

```text
control authority != independent safety function
attachment != identity != current serviceability
stimulus/readback != independent physical observation
```

F05 does not create a current device registry. When hardware arrives, Workstation supplies the node-local USB/endpoint/driver binding while Laboratory judges experiment fitness.

### F11 / F12 / F14

Retain three foundations that the first physical experiment actively consumes:

```text
physical object = instance + state + lineage
Traceability != FitnessForPurpose
raw evidence/provenance continuity != new evidence authority
```

Historical location/environment examples inside the exact files are not current Reality. Current object identity, calibration/fitness, measurement validity and execution provenance must be observed at the actual experiment cut.

## Deliberately not restored

- **F01 facility** — current site truth must be re-observed; acquisition reconciliation/staging carries only the surviving facility boundary.
- **F03/F04/F07/F08/F09/F10/F13/F15** — useful family research, but not current minimum source dependencies.
- **Agent-first architecture / Agent-realizability axis / synthetic→physical map** — their useful structure has already been consumed into the current synthetic engineering floor; their dated operational standings would conflict with newer P5/P6 results.
- **Minimal Persistent Reality Gateway** — lower-bound idea remains useful, but its locality/procurement standing is superseded by current R0 staging.
- **E1 geometry freeze** — E1 remains JIT and inventory-dependent.
- **dated product/locality/inventory audits** — remain historical because their very subject is currentness-sensitive.

## Rule

```text
Canonicalize derivation carrier
!= revive dated operational standing
```

Future restoration from detached Laboratory history should require a current consumer/source dependency, not document-count symmetry.

The remaining detached Laboratory corpus is dispositioned in `LABORATORY-DETACHED-RESEARCH-CARRIER-AUDIT-CLOSEOUT-20260828.md`; historical predecessor/companion mentions are explicitly distinguished from modern load-bearing dependencies, so no recursive F01–F15 restoration is implied.

Current E0-A–E0-E disposition is owned by `LABORATORY-E0-PROGRAMME-CONTRACTION-20260828.md`: the historical frozen programme remains exact, while E0-E is no longer a mandatory first-programme gate after E0-B v0.3 and E0-D now needs only two controllers total.

E1 is now explicitly reconciled by `LABORATORY-E1-ADMISSION-CONTRACTION-20260828.md`: the historical programme remains a derivation source, while the detached geometry is retained as a candidate hypothesis and not restored as current apparatus authority.
