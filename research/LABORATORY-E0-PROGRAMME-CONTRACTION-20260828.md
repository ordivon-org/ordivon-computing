# Laboratory E0 Programme Contraction — 2026-08-28

## Standing

**E0 PROGRAMME CONTRACTED AFTER E0-B v0.3 / UNIQUE FALSIFIERS RETAINED / REDUNDANT GATE DEFERRED**

The restored 2026-08-27 programme froze E0-A through E0-E before the current E0-B information-contrast work existed. `FROZEN` protects pre-data reasoning from casual rewriting; it does not require executing a now-redundant experiment when later pre-physical research changes the blocking cut. The historical file remains exact and unchanged.

## Coverage after E0-B v0.3

| Experiment | Current disposition | Unique remaining job | New major carrier |
|---|---|---|---|
| E0-B | **PRIMARY R0** | active physical model discrimination + measurement-information contrast | R0 set |
| E0-A | **RETAIN / R0 reuse** | deliberately create `command/ack ≠ load-side effect` contradiction | none |
| E0-C | **RETAIN / R0 reuse** | response-loss `UNKNOWN → Reality reconcile`, no blind retry | none |
| E0-D | **RETAIN / pre-E1** | controller A→B physical-generation replacement/currentness | **one extra Pico** |
| E0-E | **DEFER replication** | independent DMM-vs-waveform cross-modality replication | none if R0 set present |

## E0-A — keep the negative control

E0-B already refuses to treat GPIO command acknowledgement as physical input truth; it captures actual `Vin`. But that is not the same as producing a real contradiction. E0-A still earns one small control:

```text
command/controller-side state says HIGH
+ load path intentionally disconnected/altered
+ independent load-side observation
→ intended realized effect absent/contradicted
```

This turns `Command/Ack != PhysicalEffect` from architecture into bounded physical evidence. No new major instrument is required beyond R0.

## E0-C — retain the unique recovery falsifier

E0-C remains orthogonal to E0-B. It asks what happens when dispatch may have caused a persistent physical effect but the success response disappears.

```text
possible dispatch
→ response deliberately lost
→ effect = UNKNOWN
→ no blind retry
→ independent physical observation
→ resolved state
```

The archive must retain `UNKNOWN at T1` and the later resolution at `T2` rather than rewriting history. This can reuse the R0 Pico/AD3 path with a cheap latched low-voltage load.

## E0-D — keep the claim, delete excess prerequisite quantity

Generation replacement is still unique:

```text
logical role same
firmware may be same
physical controller A != controller B
old current state must not transfer
```

But the experiment needs **two**, not four, same-SKU controllers. Current minimum before E1 is therefore one additional Pico 2 after R0, for two total. The historical ×4 pool remains rational only when parallel fixtures, immediate spares, destructive carriers or broader replacement pressure actually appear.

## E0-E — foundational claim already covered; retain as replication

The old E0-E compared a plausible DMM average with waveform observation to prove that measurement fitness is target-relative. E0-B v0.3 now exercises the deeper information distinction directly:

```text
STATIC_ONLY -> STATIC_AMBIGUOUS
ACTIVE_DYNAMIC -> unique model
```

Therefore `measurement-valid != information-sufficient-for-this-question` no longer needs E0-E as a mandatory first-programme gate. E0-E remains useful when we want a stronger **cross-instrument / cross-modality replication** — for example a PWM question where a DMM gives an honest average but cannot establish timing/duty structure.

It should reopen under real PWM/timing, cross-instrument adequacy or R1A purchase pressure, not automatically.

## Current E0 closure before E1

Minimum current pre-E1 physical programme becomes:

```text
E0-B primary physical information bridge
+ E0-A command/effect contradiction control
+ E0-C response-loss Reality reconciliation
+ E0-D two-controller generation replacement
+ low-energy safety / identity / raw-evidence re-entry
```

E0-E is not a mandatory closure gate because E0-B now supplies the target-relative measurement-information evidence.

This also means E0 closure alone does **not** justify buying SDS824X HD, SDM3055X-E or SPD4323X. Those remain R1 carriers whose own consumers must promote them.

E1 activation and apparatus geometry are separately contracted by `LABORATORY-E1-ADMISSION-CONTRACTION-20260828.md`: only four cross-physics claims remain unique, and historical numeric/product geometry is not current authority before E0 consequence and inventory Reality.
