# Laboratory E1 Admission Contraction — 2026-08-28

## Standing

**E1 DORMANT / JIT — RELATIONAL HYPOTHESIS RETAINED, NUMERIC + PRODUCT GEOMETRY UNFROZEN**

The restored frozen programme contains a useful electromechanical question family, but its companion 2026-08-27 geometry was created before the current E0 contraction, current physical inventory, and any E0 physical consequence existed. The right object to preserve now is therefore **the relation to be falsified**, not `NEMA-8 + 30–40 mm lever + 500 g load cell` as current apparatus authority.

The detached geometry remains historical candidate design. It is not restored into current main.

## E1 activation gate

E1 does not activate merely because E0 was planned. Before E1 procurement/build, all of the following must be true:

1. the contracted E0 seams have produced physical evidence for E0-B, E0-A, E0-C and E0-D with fresh-Agent re-entry;
2. a remaining cross-physics consumer actually needs motion/force/contact/energy evidence that E0 cannot express;
3. current inventory, site/bench geometry, observation carriers and low-energy safety envelope are re-observed;
4. a new **pre-data apparatus contract** is compiled from those current inputs.

Historical location, camera availability, motor class and fixture assumptions do not pass this gate.

## What E1 uniquely needs to test

### X1 — force changes the interpretation of motion

A bounded apparatus should make free motion, contact, compliant loading and one safe blocked condition distinguishable using actual displacement plus force. The key falsifier is not “a robot moved”; it is:

```text
position/motion evidence alone
!= sufficient interaction-state evidence

force + motion
→ can distinguish free/contact/compliant/blocked states
```

### X2 — actuator energy has an independent authority

A physical energy-cut path must be distinct from software stop or controller command. Ordinary Runtime/controller liveness must not defeat the cut or silently revive actuator energy.

### X3 — Reality can change outside controller authority

After de-energizing the actuator, externally move the bounded mechanism. Historical logical position then becomes stale. Fresh observation is required before renewed motion authority.

This is stronger than another device reset: it shows the physical world can evolve outside the controller's causal path.

### X4 — mechanical succession can invalidate calibration/currentness

Reversibly disturb/remount one measurement-critical contact or fixture element, retain the old qualification historically, then run reference/geometry checks to establish a new current standing.

## Retained relational geometry

A future first E1 should preserve only the relations needed by those claims:

```text
bounded low-energy actuation
+ independently observed actual displacement
+ force path that adds information beyond position
+ physical hard stops
+ independent actuator-energy cut
+ calibration/reference path through the measurement-critical chain
+ reversible identity-bearing contact/specimen
+ raw evidence suitable for fresh-Agent re-entry
```

These are constraints on **what evidence the apparatus must make possible**, not a drawing.

## What is explicitly unfrozen

No current commitment is made to:

```text
NEMA-8 or any actuator technology/SKU
30–40 mm lever radius
500 g load-cell class
driver model/interface
integrated/external camera
force-sensor/readout topology
specimen material
base/plate/bracket dimensions
mass-reference set
cut-switch hardware
fasteners / ambient sensor / fabrication route
```

At activation, choose these from current measurands, actual available inventory, site constraints, safety envelope and measurement-information requirements.

## First E1 programme after activation

The historical ten-state sequence is no longer mandatory. The current minimum unique set is:

```text
E1-U1  free/contact/compliant/blocked force-motion contrast
E1-U2  independent actuator-energy cut
E1-U3  de-energized external displacement -> stale logical position
E1-U4  remount -> requalification/currentness
```

Mechanical response-loss becomes a replication of E0-C; explicit controller reset is optional unless the real driver/controller exposes a new failure mode; specimen replacement waits for an object/material-succession consumer.

## Acquisition consequence

E1 remains **R2 / JIT**. This contraction creates no purchase line, does not restore the detached geometry file, and does not preselect a motor, load cell, camera or fixture system.

The next E1 design should be compiled **after Reality has taught us something through E0**, rather than forcing Reality to fit a drawing frozen before the first physical experiment.

`LABORATORY-PREPHYSICAL-DESIGN-CLOSEOUT-20260828.md` now places this dormant E1 behind the same Reality boundary: no E1 compilation occurs until R0/E0 evidence and a unique cross-physics consumer exist.
