# Laboratory Pre-physical Design Closeout — 2026-08-28

## Standing

**PRE-PHYSICAL DESIGN PHASE CLOSED / MINIMUM BLOCKING CUT = FRESH R0 PHYSICAL-CARRIER REALITY**

The Laboratory now has enough pre-data structure to stop designing around hardware that has not been observed. Continuing merely because more contracts, SDK glue or apparatus detail can be written would now increase speculative generation debt rather than increase present capability.

## What is already closed

The current lineage now carries the minimum synthetic floor, exact first physical bridge predictions, measurement-information falsifier, blinding and arrival gates; pressure-driven acquisition staging; the load-bearing physical derivation kernel; contracted E0 programme; and a dormant/JIT E1 whose relational claims survive without freezing motor/load-cell/camera geometry.

No current source-carrier gap requires another history-recovery pass.

## The actual blocking cut

The next missing fact is not “which SDK should we install?” or “which motor should E1 use?” It is:

```text
Do exact R0 physical carriers currently exist and become observable/admissible?
```

Current standing is **UNKNOWN / not admitted**. The local machine exposing no Laboratory USB endpoint does not prove a disconnected instrument is absent. Likewise the 2026-08-27 `PURCHASE NOT YET EXECUTED` receipt is only the last captured effect; it does not prove that no off-surface purchase occurred later.

Three paths may satisfy the cut:

```text
OWNED_ALREADY
  fresh owner/inventory evidence shows suitable R0 objects exist

CHECKOUT_AND_ARRIVAL
  explicit transaction intent
  -> fresh currentness
  -> Human-authorized payment
  -> arrival evidence

SUBSTITUTE_CARRIER
  a current carrier can realize one R0 role
  -> reopen only that role fitness before physical data
```

Purchase is therefore one possible bridge to Reality, not an inferred requirement.

## After the cut is crossed

Only then proceed:

```text
physical object identity
-> Workstation USB/endpoint/driver binding
-> minimum provider generation selected against actual hardware
-> reconnect/re-entry/serviceability
-> component/reference/safety/blinding/measurement admission
-> E0-B static baseline
-> E0-B active waveform classification
-> reveal / bridge adjudication
-> reuse R0 for E0-A and E0-C
-> optionally second same-SKU Pico for E0-D
-> retrospective before R1/E1 promotion
```

## Work explicitly stopped for now

Do **not** currently:

- install Pico SDK / ARM toolchain / WaveForms merely for readiness optics;
- implement E0-A/C against guessed device/provider generations;
- restore/freeze E1 geometry or select its motor/load-cell/camera/fixture;
- turn historical SDS824X/SDM3055X-E/SPD4323X approval into purchase necessity;
- expand PCB layout/3D-model/full-schematic simulation/FEA/CFD/provider tournaments without a consumer;
- restore more detached Laboratory research for symmetry;
- refresh price/stock continuously when no transaction is imminent.

## Reopen rule

The pre-physical phase reopens only on **new Reality or concrete consumer pressure**: fresh R0 inventory/arrival evidence, an explicit checkout request, a real pre-arrival build consumer, a capability the synthetic floor cannot express, contradictory safety/device/measurement evidence, or a physical E0 consequence that changes downstream selection.

```text
More writable detail
!= More current capability
```

The correct next Laboratory information source is now Reality.
