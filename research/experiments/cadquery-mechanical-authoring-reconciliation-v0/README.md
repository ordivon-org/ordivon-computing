# CadQuery Mechanical-Authoring Current Reconciliation v0

## Standing

`P5 ROLE_VALIDATED — SCRIPTED PARAMETRIC SOLID CONSTRUCTION + REVISION + STEP EXPORT`

This reconciliation repairs a lineage gap rather than rerunning the postponed provider tournament. Historical 2026-08-27 documents stated that CadQuery 2.8.0 already had a narrow P5 role, but no pre-AEB committed P5 evidence carrier was found. The AEB CAD-v0 apparatus created in `ee13317` is therefore reused only as a frozen geometry/verifier basis and replayed under the current persistent Laboratory engineering environment.

Current CadQuery 2.8.0 reproduces all five frozen part families in both base and revision form: 10/10 STEP artifacts pass the independent geometry oracle. The current semantic rows (pass, STEP byte size, volumetric IoU, volume error and bbox error) equal the historical apparatus-validation rows. Two current negative controls also remain discriminating: a plate with all four holes omitted and a plate with +2 mm length are rejected by the same frozen oracle.

## Why this is P5 but not a tournament result

The claimed role is bounded:

```text
structured parametric task
→ CadQuery solid construction
→ parameter revision
→ STEP export
→ independent volumetric/bounding-box verification
```

The reference candidate was authored alongside the original verifier. It therefore validates the provider/role and apparatus under current execution, but it does **not** establish that CadQuery is a better canonical authoring language/provider than build123d. That comparison remains unopened unless a real consumer needs a winner.

## Currentness and contraction

Exact historical apparatus bytes are retained here, but Python files use `.py.archive` so a closed historical runner does not re-enter current executable research surface. To replay, materialize those bytes into a separately admitted transient/current execution environment. Generated STEP files remain reconstructible execution artifacts and are retained by digest in `result.json`, consistent with the original CAD-v0 closeout.

## Boundary

Synthetic geometry validity is not manufacturability, tolerance, material, load, metrology or physical truth. P5 here is role-relative to scripted parametric solid construction/revision and STEP export only.
