# P0–P3 Production Consumption C-Series — C0 Inventory

Status: frozen consumer-pressure inventory for C1–C4. This is derived research evidence, not owner truth.

## Owner-current sources

| Consumer | Host Task | Observed revision | Current pressure | C-series role |
| --- | --- | ---: | --- | --- |
| Game | `task:game:station-zero-consumer-development-r1-20260814` | 13 | R11 strategic viability after R1–R10 consumer loop completion | Positive control: owner-native next action is already explicit; new infrastructure should not help much |
| Finance | `task:finance-distressed-position-rescue-20260814` | 29 | Typed governed SNXX reduce-only effect boundary still absent; connector remains GET-only; fresh pre-effect observation required | High-value P0/P1/P3 authority/currentness test; **read-only experiment only, no live order** |
| Security | `task:security:classical-adversarial-capability-root-20260814` | 8 terminal | CA0–CA7 closed; future work must start from new pressure, not automatic roadmap continuation | Closed-domain holdout / anti-infrastructure-inflation control |
| Studio | `task:studio:consumer-production-r1-20260814` rev3 + `task:studio:capability-expansion-tool-consumption-20260814` rev3 | 3 / 3 | Ordinary R2 should move to motion/video using current stack; Figma remains ungraduated pending OAuth | Optional-capability authority test: blocked Figma must not block other admitted production paths |

## Mechanism mapping

### P0 — capability/composition exposure

Potential value:
- make Installed → Run-admitted → Turn-admitted staging explicit;
- expose that an installed/known mechanism is not current action authority;
- reduce wrong-surface selection and source-code archaeology.

Expected strongest consumer: Finance. Positive controls: Game and Security should often remain owner-native.

### P1 — reusable exact procedural capital

Frozen reusable procedure for C2:

> Before depending on a capability or effect, distinguish installed/discovered capability from exact current admission; re-read the authoritative owner when currentness matters; an unavailable optional capability must not block an already-authorized native path that satisfies the objective; UNKNOWN routes to owner reconciliation before any new effect identity; physical success is not semantic completion.

The same exact external procedure reference/digest is selected into independent Harness Runs. Harness does not discover, rank, auto-inject, evaluate or promote it.

### P2 — programmatic Tool composition

Independent-consumer admission rule:
- only count P2 as independently consumed if an existing real consumer has an observation-dependent multi-Tool chain over an already-existing exact Tool surface;
- do **not** invent a Game/Finance/Studio/Security ToolBridge merely to produce a positive result;
- if no such consumer exists in this round, keep P2 advanced and record `independent-consumer-not-yet-earned` rather than treating repository-repair as cross-domain proof.

### P3 — owner-qualified Situation

Potential value:
- join continuity / locus / admission / occurrence / recovery / completion without centralizing owner reads;
- surface stale navigation hints, missing exact admission, physical-success-vs-semantic-completion, and UNKNOWN owner routes;
- owner reads are expected to remain unchanged; only repeated mechanical joins/unsafe implications should fall.

## Frozen model plan

- C1/C2 primary: `deepseek-v4-flash`, 5 replicates per case/treatment.
- C3 holdout: `deepseek-v4-pro`, 2 replicates per case/treatment after packets and oracle are frozen.
- Treatments: `raw`, `p0p3`, `p1`, `full`.
- Mechanical/authority/recovery assertions remain deterministic; model output is never the correctness oracle.
- If a cell is unstable after the frozen run, variance expansion is allowed without changing prompts, packet, options or scoring.

## Acceptance logic

A mechanism may be retained at its current maturity when it reduces mechanical friction or unsafe implications without weakening owner reads/evidence. Further promotion requires materially different consumer pressure. No P4 is admitted by this round itself.
