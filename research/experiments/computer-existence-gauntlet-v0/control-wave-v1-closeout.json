{
  "deletionEvidenceRef": "evidence/control-deletion-v1.json",
  "forbiddenConclusions": [
    "Do not delete every checker merely because semantic kill rate is zero.",
    "Do not hard-code all current prose into checkers as a substitute for independent oracles.",
    "Do not treat internal self-reference as sufficient existence evidence.",
    "Do not repair current faults before preserving this baseline."
  ],
  "headline": {
    "positiveControlKilled": true,
    "realCurrentStateFaults": 4,
    "semanticKillRate": 0.0,
    "semanticMutantsKilled": 0,
    "semanticMutantsTotal": 5
  },
  "kind": "ordivon.computer-control-wave-v1-closeout",
  "mutationEvidenceRef": "evidence/control-mutation-v1.json",
  "nextWave": "PROTOCOL molecular ablation; CONTROL repairs, if any, must be justified by mutant kill coverage and trigger-cost evidence rather than more narrative.",
  "preliminaryDispositions": {
    "CTRL-01": "inconclusive — compressed Core world model requires Agent task ablation in METHOD/COMPOSED wave, not text-only checker evidence.",
    "CTRL-02": "narrow — retain one canonical current-status source; generated Markdown is projection and question representation still requires compression attack.",
    "CTRL-03": "narrow_or_archive_relations — relation graph has no mechanical consumer and contains a P4-bypassed prerequisite; question registry function may remain if simpler than portfolio-only lookup.",
    "CTRL-04": "inconclusive — internally consumed but semantic authority inversion survives; must face task-local manifest + Git baseline.",
    "CTRL-05": "narrow — current checker proves record identity/freshness structure, not the semantic world-model law; semantic inversion survived.",
    "CTRL-06": "narrow — semantic portfolio freshness binding is proven useful, but decisionRule semantic inversion survived.",
    "CTRL-07": "narrow — validators currently prove structural/integrity contracts only; green status must not be represented as semantic correctness until mutation kill coverage exists.",
    "CTRL-08": "narrow_or_repair — deterministic CI exists but critical authority/checker paths do not trigger it.",
    "CTRL-09": "inconclusive — historical compression has an explicit Git-recoverability contract but must face active-tree removal/restore experiments."
  },
  "schemaVersion": 1,
  "sourceRevision": "8f9fccc9d6c0b8361c27138b053c1cc0b5d6e37c",
  "status": "control_plane_failed_semantic_self-defense"
}
