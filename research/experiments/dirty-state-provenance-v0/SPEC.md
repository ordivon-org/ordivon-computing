# Dirty State Provenance / Lifecycle Adversarial Audit v0

Question: has Ordivon outgrown a binary `dirty -> preserve/do not touch` interpretation, and if so what is the smallest safe semantic disposition model that preserves loss-prevention?

Review must not propose age-only deletion or let Runtime infer domain intent. It must distinguish canonical shared roots from isolated detached Runtime Workspaces, unique-byte recoverability from semantic claimant, and historical validity from current intent. A new Host/Runtime field/tool is not earned unless existing checkpoints + Runtime projections cannot support the required decision safely.
