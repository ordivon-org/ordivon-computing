[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ordivon-external-semantic-contract-conformance"
version = "0.1.0"
description = "Conformance, fixtures, integration adapters, and evidence for the promoted Ordivon protocol package."
requires-python = ">=3.12,<3.13"
license = { text = "Apache-2.0" }
dependencies = ["ordivon-protocol==0.2.0"]

[tool.setuptools]
py-modules = []
