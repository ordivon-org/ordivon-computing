# OFR7 Closeout

## Status

**COMPLETE — marginal OFR6 practical value was not identified at the frozen baseline ceiling; no OFR6 Core/Knowledge claim is semantically falsified; full Foundations are rejected as default per-decision Context for this workload class.**

OFR7 froze 24 owner-grounded scenario cases before live generation, used 4 development cases only for apparatus repair, and never changed the 20 holdout cases, gold, treatment text, thresholds, or treatment set. Development exposed one purely mechanical RunBudget problem: the 40k token ceiling rejected full doctrine before any Provider attempt. Raising only the Harness budget to 100k produced 32/32 valid development generations and 4/4 valid judges.

The untouched holdout then produced 159/160 valid generation results and 20/20 valid treatment-blind judge calls. DIRECT, PRE_OFR6_FULL and POST_OFR6_FULL were all exactly 1.0 on primary decisions and causal grades for both DeepSeek Flash and Pro. POST_FULL therefore created zero paired corrections over PRE_FULL and zero regressions, while costing substantially more Context. DIRECT also matched the ceiling at the smallest cost.

The one Focused failure remains counted; three exact diagnostic retries succeeded and are explicitly non-replacement evidence. Auxiliary truth/evidence/seek fields were diagnosed as underidentified and are not retroactively rewritten.

## What OFR7 changes

It does **not** revert C2, C4, C10, M13, or M16. Their target families reached ceiling under the already-strong baseline, so their marginal effect is unidentified rather than negative. M17 receives stronger direct treatment support.

It does change consumption doctrine: a full shared world-model surface is not justified as default Context when owner-current facts already make the action boundary explicit. This reusable boundary is recorded in `knowledge/agents/causal-responsibility-explanation.md`; Core remains unchanged because A6, A11 and A17 already generate the rule.

## What remains unresolved

OFR7's multiple-choice cases deliberately made the owner facts explicit. It therefore cannot answer whether Foundations help when the Agent must first discover which causal distinction is missing, select owner evidence under noisy retrieval, or construct a representation for an under-specified research problem. That must be a new frozen experiment, not a post-hoc harder OFR7.

## Validation

Final receipt: `validation-v1.json`. The semantic freeze remained byte-identical; the 159/160 primary realization result remained unmodified by diagnostics; all 12 owner heads were still exactly at the freeze and clean; repository conformance passed.
