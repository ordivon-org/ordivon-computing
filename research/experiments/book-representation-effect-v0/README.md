# Book Representation Effect v0

Status: **completed current system-level phase; Media Book remains NO_CHANGE**.

This directory tests representation effects across **consumer × operation × capability/model variant × persistence path** after the five-chapter Book and Result/Value falsifier established that explicitness is neither automatically necessary nor automatically useless.

Core standing:

```text
RepresentationEffect
= f(consumer, operation, capability/resources, source search geometry,
    persistence/interference path, target relation, consequence)
```

Start here:

1. [`RESULTS.md`](RESULTS.md) — integrated result and destination decision.
2. Historical verifier note — the former executable `verify.py` was removed from the active research tree after the required ANC-REPRESENTATION-001 natural replication failed; its exact historical bytes remain Git-recoverable.
3. [`SOURCE-FENCE.json`](SOURCE-FENCE.json) — exact owner/input cut.
4. [`EXTERNAL-PRESSURE.md`](EXTERNAL-PRESSURE.md) — external solution-domain constraints.
5. [`A-model-variant/`](A-model-variant/) — same frozen reconstruction workload under current DeepSeek provider variants.
6. [`B-persistent-trace/`](B-persistent-trace/) — acquisition → 2200-byte trace → unrelated work → fresh recovery.
7. [`C-natural-result-audit/`](C-natural-result-audit/) — real Computing research artifacts and operation-relative source projection.
8. [`D-human-protocol/`](D-human-protocol/) — Human transport gate and dormant residual protocol; no Human outcome claim.

Headline results:

- `deepseek-v4-flash`: Book `10/24` exact, 28 label errors; Map+Book `12/24`, 14 errors.
- `deepseek-v4-pro`: Book `12/24`, 19 errors; Map+Book `20/24`, 4 errors. This is workload-specific model-variant sensitivity, not a universal capability ordering.
- persistent 2200-byte traces: Book-trace `11/24`, 20 errors; Map+Book-trace `11/24`, 26 errors.
- natural full-source audit: Book `73/90`; Map+Book `72/90`.
- natural operation-relative evidence cards: Book+cards `77/90` at a smaller prompt; post-result Map+Book+cards fell to `67/90`.
- the natural oracle's two Consumption claims and one P5 RealizedBenefit claim were challenged after the run; the immutable raw oracle is retained and a sensitivity view is reported rather than silently rescored.

No universal representation service, Result ontology injection, Chapter 6, default persistent taxonomy trace, or direct Human experiment is authorized by this programme.
