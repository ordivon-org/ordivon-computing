# Post-result exploratory combination check

Status: exploratory only. Designed after the full-packet and evidence-card results were observed; it cannot retroactively change their admission criteria.

Question: are the compact Result/Value taxonomy and operation-relative evidence projection complementary once source search geometry has already been improved?

Arm:

```text
COMPACT-MAP + exact five-chapter Book + frozen evidence-cards
```

The cards and original oracle remain byte-identical. Three fresh `deepseek-v4-flash` Runs, zero Tools, same structured audit contract.

Interpretation:

- improvement over `Book + cards` suggests a possible interaction worth later confirmatory testing;
- no improvement means the taxonomy is redundant once evidence is projected for this operation;
- the original oracle contains three challenged strong fields (`CEL.currentConsumption`, `P5.currentConsumption`, `P5.realizedBenefit`), so both raw predeclared score and a declared sensitivity view that excludes those fields must be reported. No silent rescore is allowed.
