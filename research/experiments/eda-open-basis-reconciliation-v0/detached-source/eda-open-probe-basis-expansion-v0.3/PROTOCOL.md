# EDA Open Probe Basis Expansion v0.3 — preregistration

v0.3 follows a valid but non-completing v0.2 (3/3 timeout/no candidate) whose session forensics showed dominant effort in deterministic KiCad Standing reconstruction. The intervention is **Standing compilation**, not answer disclosure.

Each cell contains:
- the same baseline KiCad project;
- the same two hypotheses and current `remove_wire`-basis insufficiency;
- `BASELINE-STANDING.json`, produced before live execution by KiCad Target Standing Adapter v0.1;
- no prior mutation enumeration, no known separator coordinates, no label-mutation grammar and no prior hand-authored witness.

Standing exposes current ERC cleanliness, target net membership/name, uniquely bound target pin coordinates and directly incident wire witnesses. It proposes no mutation operator.

Harness remains the corrected v0.2 process-cwd binding (`cd CELL && hermes ...`). Three fresh cells, Hermes safe-mode, DeepSeek `deepseek-v4-flash`, reasoning low, terminal+file, max 360s.

Primary semantic witness metric is unchanged from v0.2: provider parse + ERC clean + target connectivity preserved + target net name changed, causing the two hypotheses to disagree. Delivery contract remains separate.

Additional diagnostic: time/API/tool burden to first project mutation, if any, compared with v0.2 trajectory.

Boundary: success would show that responsibility-correct Standing projection unlocks basis-expansion execution in this fixture. It would not mean Standing should contain the answer or that arbitrary EDA mutation invention is solved.
