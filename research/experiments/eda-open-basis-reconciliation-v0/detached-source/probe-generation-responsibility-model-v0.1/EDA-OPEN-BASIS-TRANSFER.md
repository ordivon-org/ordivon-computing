# EDA Open-Basis Transfer — 2026-08-28

The open-basis experiment adds a missing causal link to the earlier bounded EDA mutation enumeration.

1. `remove_wire`-only generation basis was independently proven insufficient: zero separating mutations.
2. Raw-project clean-Agent v0.2 spent its budget reconstructing KiCad Standing and produced no candidate Effect.
3. Standing-compiled v0.3 allowed one of three clean Agent episodes to independently introduce a local label (`GRID_A`) not named in its task/context.
4. Provider verification established that this single edit preserved target connectivity/ERC and changed only the relevant net-name representation, separating the two frozen hypotheses.
5. That witness falls inside the previously realized bounded `add_local_label_at_wire_endpoint` grammar, whose deterministic provider-backed enumeration produces 55 label candidates and six equivalent separators.
6. Capability Assimilation Gate therefore admits the bounded EDA generation/selection responsibility as `assimilate_deterministic_default`.

The result supports a dynamic frontier model:

```text
current grammar insufficient
→ Agent/domain synthesis proposes a witness outside current basis
→ provider consequence verifies it
→ extract stable operator family
→ enumerate / verify bounded grammar
→ assimilate repeated generation + selection
→ frontier moves to grammar forms still absent
```

Crucially, `Effect` and `Delivery` remain distinct. The successful Agent episode timed out without its required proposal receipt even though its source Effect and provider consequence were valid. This is not a reason to discard the witness; it is a reason to move deterministic verification and delivery receipt construction out of the Agent's open-ended cognition loop.
