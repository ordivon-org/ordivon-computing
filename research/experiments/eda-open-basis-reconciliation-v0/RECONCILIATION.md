# EDA Open-Basis Detached-Lineage Reconciliation — 2026-08-28

## Disposition

`HISTORICAL_EXACT_ARCHIVE / CURRENT_PROVIDER_REPLAYABLE / NO_FULL_EDA_P5_PROMOTION`

This canonical Computing archive preserves the bounded EDA open-basis result from detached revision `22f6c33fd7826e65d5166525a9d906a96dfc8dc7` without merging that divergent research world into current Computing authority.

Current canonical base at reconciliation was `6b99bbd274226d8685a055f804d70033a5af4a43`. The detached EDA revision is not its ancestor. Current main also no longer carries the original AEB KiCad baseline, mutation enumerator, probe compiler, standing adapter, or open-basis experiment directories. Therefore cherry-picking only `22f6c33` would restore result files without the evidence ecology needed to interpret or replay them.

## What is preserved

The archive retains exact bytes for the smallest coherent evidence chain used by the result:

- KiCad 10.0.5 provider admission receipt plus the frozen baseline project;
- the bounded mutation grammar `{add_local_label_at_wire_endpoint, remove_wire}` and its provider-backed enumeration/separation outputs;
- the bounded deterministic probe-generation compiler prototype;
- Standing-compiled open-basis experiment v0.3, including all three trials and the original verifier;
- the fixture-scoped KiCad target Standing adapter;
- the probe-generation responsibility/assimilation model;
- the exact historical Agent-produced `GRID_A` schematic Effect that survived outside Git under `/tmp`.

The historical `GRID_A` source digest is:

`sha256:94c256be62b054584e02242e263f4c6b128073ce902e9c29246f3cff0435fff3`

It is now retained at `exact-grid-a-witness/ecc83-pp_v2.kicad_sch` rather than depending on ephemeral `/tmp` survival.

## Reconciled standing

The bounded result remains:

```text
remove_wire-only deterministic basis
-> no separator for the frozen hypothesis pair

clean Agent with current Standing exposed
-> independently proposes a local-label Effect outside the named basis

provider verification
-> ERC electrical cleanliness preserved
-> R1.1/U1.1/U1.7 connectivity preserved
-> target net name Net-(U1A-G) -> /GRID_A
-> H_connectivity_semantic accepts
-> H_exact_net_name rejects
```

The Agent episode still failed its delivery contract because it timed out without `proposal.json`. That distinction is retained:

`Effect succeeded != Delivery succeeded`.

The useful operator family was already mechanically realizable after discovery. Within the frozen grammar, deterministic enumeration records 96 candidates, including 55 local-label candidates and six separating candidates. The research disposition was therefore to assimilate repeated bounded generation/selection as deterministic default while moving open Agent cognition to grammar forms still absent.

## Currentness boundary

This archive is historical evidence plus a retained current-replay receipt. It does **not** claim that a historical provider remains current. During reconciliation, a transient runner independently checked the locally materialized KiCad wrapper/AppImage digests against the archived provider receipt and replayed the exact witness; the resulting `current-replay-20260828.json` is retained, while the runner itself is not kept as unbound live research apparatus.

The retained successful replay establishes only that the archived semantic witness remained valid under that exact locally observed provider generation on 2026-08-28. It does not establish physical circuit correctness, universal KiCad parser correctness, a universal EDA mutation language, or full Agent-operated EDA authoring.

## Why this is an archive rather than a merge

Current Computing contraction explicitly allows divergent positive research to be preserved as exact canonical history when its evidence matters but its whole research world is not current authority. The prior Representation reconciliation established this pattern. This EDA archive follows the same rule:

```text
Detached useful evidence
!= merge entire detached world

Historical exact-byte retention
!= active owner authority

Replayability
!= product/capability promotion
```

The full 93-commit detached side therefore remains outside current active Computing main. Only the evidence chain required to recover and challenge the EDA result is preserved here.

## Remaining Laboratory frontier

This reconciliation closes lineage/evidence-loss pressure only. It leaves separate current questions:

1. expose already-materialized Laboratory providers through the Workstation node-local capability surface without granting domain admission;
2. keep the 2026-08-28 current grammar replay receipt as the timing/currentness coordinate: the 96-candidate output is byte-identical to history, while full enumeration took 58,499 ms under the current Runtime and the historical unlabeled `1200 ms` field is not transported as equivalent wall time;
3. current deterministic lowering is now replayed through ERC, PCB DRC/parity, BOM, Gerber, Drill and PCB-body STEP; component-complete STEP remains open because the frozen fixture intentionally excludes its referenced 3D-model environment closure; the remaining authoring frontier is structured design intent/revision/recovery rather than fabrication export mechanics;
4. bridge electronic design intent/consequence to ngspice and later physical evidence under owner-specific admission.

## Executable-apparatus contraction

The historical Python apparatus is retained byte-for-byte under `.py.archive` names and mapped back to its exact detached Git paths/blobs in `historical-executable-apparatus-map-v0.json`. This is deliberate: current Computing conformance requires executable files under `research/experiments/` to be bound to live portfolio work. The detached EDA experiments are closed historical evidence, so restoring their executable suffixes would incorrectly recreate live research surface. Future reconstruction may materialize the archived bytes under a separately admitted current experiment, but the archive itself grants no execution authority.
