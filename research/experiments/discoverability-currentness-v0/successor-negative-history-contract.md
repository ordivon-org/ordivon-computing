# Successor / Negative-History Discoverability Controls — 2026-08-26

Status: **pre-frozen before Atlas control retrieval**.

Purpose: test whether historical names, real former mechanisms, physical repository moves, and superseded owner structures can be recovered without being silently reactivated as current truth.

## Oracle rule

Atlas is only the candidate instrument. Currentness is adjudicated against current committed owner-native / current cross-owner authority surfaces. Historical sources remain valid evidence of what was once true; they do not become current merely because retrieval ranks them highly.

```text
HistoricalValidity != Currentness
CandidateDiscovery != SuccessorResolution
SemanticSuccessor != SamePhysicalRepository
HistoricalMechanismWasUseful != MechanismStillCurrent
```

## Frozen controls

### C1 — Network -> Interlocus same-owner successor

Historical query:

`Network owner communicability cross-locus capability why rename Network`

Current oracle:

- current `/root/projects/ordivon-interlocus` authority publications state: `Owner identity remains research-owner:network across the Network -> Interlocus current-name transition.`
- current project name/referent is Interlocus / Interlocus Capability.

Required successful recovery:

- historical Network is recoverable;
- current successor is Interlocus;
- same stable semantic owner identity is preserved;
- old name must not be presented as a distinct still-current sibling owner.

### C2 — Normative physical migration without semantic-owner change

Historical query:

`Normative owners/ordivon-normative shared research repository physical home`

Current oracle:

- `/root/projects/ordivon-normative/PHYSICAL-MIGRATION-20260822.md` says the standalone move changes physical placement and Git topology only; semantic owner identity/referent/standing are unchanged.
- `/root/projects/ordivon-research/README.md` says Interlocus and Normative moved to standalone repositories on 2026-08-22; their old shared-corpus locations are non-authoritative migration tombstones.

Required successful recovery:

- current standalone `ordivon-normative` is the physical home;
- migration does not mint a new semantic owner;
- old shared location is historical/tombstone, not current owner source.

### C3 — Post-Host partial successor + retained sibling history

Historical query:

`Post-Host PHR1 PHR2 PHR3 PHR4 current owner`

Current oracle:

- `/root/projects/ordivon-normative/HISTORY-PHR1-ORIGIN.md`: PHR1 is the only surviving independent owner from the Post-Host trajectory and historical origin of current Ordivon Normative.
- PHR2–PHR4 are deliberately not active Normative subprojects; canonical histories live in sibling `ordivon-research/history/post-host-research-history/`.

Required successful recovery:

- PHR1 -> current Normative lineage;
- PHR2–PHR4 remain recoverable historical sibling research;
- no claim that the whole Post-Host sequence is current Normative structure.

### C4 — Workstation no-current-standalone-owner / rehome control

Historical query:

`Ordivon Workstation current owner repository semantic MCP physical realization binding`

Current oracle:

- `/root/projects/ordivon-computing/research/BOOK-V05-INTEGRATION-AUDIT-20260825.md`: `No current Workstation repository exists as an independent owner in /root/projects; older Workstation evidence remains historical/owner-rehome context rather than a current Book-domain row.`
- `/root/projects/ordivon-computing/research/COMPUTER-AGENT-NATIVE-LINEAGE-20260724-20260825.md`: Workstation was re-represented as Execution-Node Substrate & Physical Realization/Binding Authority; public MCP retired; external Ordivon control surface remained Host + Runtime, while Workstation operations were consumed internally through Runtime/domain paths.

Required successful recovery:

- historical Workstation evidence remains discoverable;
- no fabricated current standalone repository/owner;
- re-representation / retired public MCP is recoverable when current use is asked.

### C5 — GoalCoordinatorHost historical-validity / current-removal control

Historical query:

`GoalCoordinatorHost shared Goal coordination current Host`

Historical positive evidence:

- older Computing multi-participant experiments genuinely found `GoalCoordinatorHost` sufficient for their then-current minimum workload.

Current oracle:

- current `/root/projects/ordivon-host/research/40-ENGINEERING-CONSUMPTION-C1-C9.md`: C1 shared Goal coordination **removed `GoalCoordinatorHost` / Goal snapshot ownership**, retaining only generic terminal/revision kernel invariants.

Required successful recovery:

- historical sufficiency may be recovered as historically valid;
- current Host must not be reported as still owning/exposing `GoalCoordinatorHost` solely from that history;
- current removal/contraction should be legible or caller should be explicitly forced to owner-native revalidation.

## Evaluation vector

For each control record separately:

1. historical candidate recovered?;
2. current successor/removal recovered?;
3. current semantic owner resolved?;
4. historical/current distinction explicit?;
5. physical-move vs semantic-owner distinction preserved?;
6. false current activation risk in top-8?;
7. lawful owner-native escape available?;
8. final currentness status: `CURRENT_RESOLVED`, `HISTORICAL_ONLY`, `MIXED_REQUIRES_REENTRY`, `MISLEADING_REACTIVATION`, or `NOT_RECOVERED`.

## Prebound falsifiers

- C1 fails if Network is recovered but Interlocus successor is not discoverable/legible.
- C2 fails if old shared path is treated as current Normative owner source or migration is mistaken for semantic-owner replacement.
- C3 fails if PHR2–PHR4 are absorbed into current Normative structure.
- C4 fails if retrieval implies a current standalone Workstation owner/repository without current source evidence.
- C5 fails if historical GoalCoordinatorHost sufficiency outranks/obscures current removal and a finite consumer could reasonably infer the mechanism remains current.

No architecture change is admitted by these controls.
