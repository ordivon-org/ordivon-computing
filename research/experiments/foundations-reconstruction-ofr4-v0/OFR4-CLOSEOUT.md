# OFR4 Closeout

## Status

**COMPLETE AS A FALSIFICATION / NO-PROMOTION ROUND.**

OFR4 froze four representations before live execution, ran two development wiring campaigns and one untouched six-case holdout through the current Harness/DeepSeek surface, retained physical failures separately from semantic grades, and did not rewrite packets/oracles after observing outcomes.

## Final disposition

```text
raw frozen selector: full_eight_role
scientific promotion: NO_THEORY_UNIT_PROMOTION
```

Reasons:

1. no compressed candidate is semantically non-inferior under the frozen role grades;
2. the raw action/reopen gate is invalid because it collapses Theory, Evidence and admission authority;
3. the full packet's survivor-perfect holdout score is missing-not-at-random: 4/12 accepted trials over only 3/6 holdout cases;
4. the negative control strongly falsifies winner-only compression;
5. the tested compression saved much less full prompt cost than packet words and sometimes shifted cost into completion/reconstruction.

## What survives into OFR5

OFR3's eight causal semantic roles remain the current conservative **recoverability target**, not eight mandatory fields. OFR4 adds four constraints:

- `reopenCondition` is descriptive/conditional, not an executable trigger;
- Theory availability does not establish Evidence sufficiency;
- Evidence sufficiency does not grant rival/action admission;
- semantic completeness and current model/Harness realization reliability must be tracked separately.

OFR5 should therefore build an Atlas/navigation/compression surface, not a Theory database or rule engine. Prefer compact prose and exact revision-bound references, and let owner-native Evidence/Authority remain outside the theory representation.

## Validation

Repository-native closeout passed 3/3 foundational-doc tests, 6/6 research-portfolio tests, 59/59 foundational Markdown checks, Python compile, JSON parsing, `git diff --check`, frozen-digest/trial-identity validation, and the Core/Knowledge no-touch fence. Exact machine receipt: `validation-v1.json`.
