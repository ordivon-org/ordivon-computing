[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ordivon-content"
version = "0.2.0"
description = "Minimal managed-document metadata validation for Ordivon repositories."
requires-python = ">=3.12"
license = { text = "Apache-2.0" }

[project.scripts]
ordivon-content = "ordivon_content.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
