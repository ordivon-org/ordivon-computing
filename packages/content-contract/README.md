# Ordivon content contract

This package contains the schema, document profiles, terminology, and optional editor/linter rules consumed by `ordivon-content`.

The contract is intentionally independent of a CMS and does not own repository facts. JSON Schemas are portable records; the P0 CLI also performs dependency-free validation so a repository check does not require network installation.
