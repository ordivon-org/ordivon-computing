# Ordivon content fixtures

Fixtures protect parser and contract behavior. `valid/` examples must pass strict checks when copied into a managed path. `invalid/` examples each retain a named failure and must not be reused as documentation.
